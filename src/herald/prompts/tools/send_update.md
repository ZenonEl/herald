Send an explicitly requested structured internal update.

Call get_writing_rules(project) for the current writing style.
The server renders summary and nonempty list fields as escaped Telegram HTML.
Fields must remain within preset-specific character and item limits.
Use send_text for free-form messages without those field limits.
