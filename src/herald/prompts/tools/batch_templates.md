List named batch templates and the default for a project. Roles and tags are
metadata, never automatically added to client text. Template contents fill text
and paths by part id. Custom part arrays are also supported.
