interface:
  display_name: "Herald Send"
  short_description: "Send copy-ready client messages via Herald"
  default_prompt: "Use $herald-send to send a copy-ready client message grouped by the real subjects they raised, without internal report headings."
