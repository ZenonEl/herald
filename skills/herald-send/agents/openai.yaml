interface:
  display_name: "Herald Send"
  short_description: "Send brief client-ready updates via Herald"
  default_prompt: "Use $herald-send to send brief client-ready facts in plain language, grouped by named object without reasoning or generalisations."
