interface:
  display_name: "Herald Send"
  short_description: "Send clear manager-ready messages via Herald"
  default_prompt: "Use $herald-send to send a freely structured Telegram HTML message that a manager without project context can understand and forward to the client."
