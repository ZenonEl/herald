interface:
  display_name: "Herald Send"
  short_description: "Send concise structured updates via Herald"
  default_prompt: "Use $herald-send to send a concise manager update with the correct project route."
