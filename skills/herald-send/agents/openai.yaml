interface:
  display_name: "Herald Send"
  short_description: "Send brief client-ready updates via Herald"
  default_prompt: "Use $herald-send to send a brief client-ready update limited to the requested subject, asking only questions that block the next action now."
